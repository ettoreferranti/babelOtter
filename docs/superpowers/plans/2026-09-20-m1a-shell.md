# M1a Shell — Capture, Replacement & the Ollama Client

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Complete M1a by adding the two halves that touch the outside world — reading and writing a selection in other applications, and streaming from the Ollama daemon over loopback — on top of the pure core delivered by `2026-09-19-m1a-core.md`.

**Architecture:** Every part of this plan is split along one line: the parsing, framing, tier-selection and policy logic is pure and lives in `BabelOtterKit` where CI can prove it; the system calls that cannot run in CI live behind narrow protocols with fake implementations for tests and one real implementation each. The Ollama client is the single file in the package permitted to reference networking APIs, and it routes exclusively through `OllamaEndpoint`.

**Tech Stack:** Swift 6, swift-testing, `ApplicationServices` / `AXUIElement`, `AppKit` (shell target only), `URLSession` with `bytes(for:)` for NDJSON streaming. No third-party dependencies.

**Spec:** `docs/superpowers/specs/2026-09-18-babelotter-design.md`.
**Measured reality:** `docs/architecture.md` §7 — written from spike #30, run against real applications on macOS 27. **§7 supersedes §2–3 where they disagree, and this plan is built on §7, not on the original two-tier model.**

## Global Constraints

- **NFR-P4 — one networking call site.** `Config/networking-allowlist.txt` permits exactly `Sources/BabelOtterKit/LLM/OllamaClient.swift`. `NetworkingCallSiteTests` fails the build on a second one. The guard matches on **spelling and does not exempt comments**, so avoid writing the flagged spellings even in prose (the core plan hit this twice).
- **NFR-P2 — loopback only.** Every request is built from an `OllamaEndpoint`, which makes a non-loopback host unrepresentable.
- **NFR-Q3 — the core imports no UI framework.** `AppKit`, `SwiftUI`, `UIKit`, `Cocoa`, `Carbon` are banned inside `BabelOtterKit`. Anything needing them lives in `Sources/BabelOtterApp`. `ApplicationServices` is **not** banned, so the AX calls themselves may live in the kit; `NSPasteboard` and `NSRunningApplication` may not.
- **Mutation score ≥ 80%.** Every new file under `Sources/BabelOtterKit` must be assigned to a mutation pass in `.github/workflows/ci.yml` or CI fails by design.
- **Avoid two muter parse bugs** (both poison a whole file, because mutant schemata compile every mutant into one binary): a `while` condition that is a comma-conjunction containing a relational operator, and a ternary whose condition ends in an enum member. Write nested `guard`s and `if`/`else` instead. See `muter.conf.yml` and the note in `PromptBuilder.languages`.
- **Verify guards by making them fail.** Every invariant below gets a planted defect and an observed failure before it is believed.

---

## What spike #30 measured, and what it obliges this plan to do

These are not background reading. Each one changes a design decision below.

1. **Capture is three tiers, not two.** `kAXSelectedTextAttribute`, then WebKit's parameterized `AXSelectedTextMarkerRange` → `AXStringForTextMarkerRange`, then the clipboard. Safari and Mail return `noValue` for tier 1 and expose the selection only through text markers. Skipping tier 2 makes Safari and Mail clipboard-only — less private and less reliable than they need to be.
2. **`AXUIElementSetAttributeValue` lies.** On Mail's read-only HTML view it returns `.success` and changes nothing. **Replacement must read back and compare before reporting success** (Task 10).
3. **Attribute presence is not capability.** VS Code advertises `AXSelectedText`, `AXSelectedTextRange`, `AXSelectedTextRanges` *and* `AXSelectedTextMarkerRange`, and returns empty from all of them. **Capability detection must attempt a real capture and require a non-empty result** (Task 8). Probing attribute names classifies VS Code as tier 1; it is tier 3.
4. **Electron needs `AXManualAccessibility` set in advance.** The tree builds asynchronously, so the first capture after enabling still fails. It must be set **when an Electron app is first seen frontmost**, not at capture time (Task 9).
5. **The clipboard path is not rare.** It is the only path for Electron and the only path for replacement into web content. This is why #77 was pulled from M4 into this milestone (Task 12).

---

## File Structure

| File | Responsibility |
|---|---|
| `Sources/BabelOtterKit/LLM/NDJSONFramer.swift` | Splits a byte stream into lines across packet boundaries (#41) |
| `Sources/BabelOtterKit/LLM/OllamaWire.swift` | Request/response shapes for `/api/chat`, `/api/tags`, `/api/pull` (#41, #45, #46) |
| `Sources/BabelOtterKit/LLM/OllamaTransport.swift` | The protocol the client talks through, plus its fake (#41) |
| `Sources/BabelOtterKit/LLM/OllamaClient.swift` | **The one permitted networking call site** (#41, #45, #46, #47) |
| `Sources/BabelOtterKit/LLM/GenerationTimeout.swift` | Timeout and cancellation policy, pure (#47) |
| `Sources/BabelOtterKit/LLM/DaemonStatus.swift` | Health states and recovery policy, pure (#45) |
| `Sources/BabelOtterKit/Capture/SelectionSnapshot.swift` | Captured text plus what is needed to write it back (#31) |
| `Sources/BabelOtterKit/Capture/CaptureTier.swift` | The tier ladder and its selection policy, pure (#31, #32) |
| `Sources/BabelOtterKit/Capture/AccessibilityReader.swift` | Tiers 1 and 2 against `AXUIElement` (#31) |
| `Sources/BabelOtterKit/Capture/ElectronAccessibility.swift` | `AXManualAccessibility`, armed on frontmost change (#31) |
| `Sources/BabelOtterKit/Capture/CapturePolicy.swift` | Which tier to try, and strict-mode refusal, pure (#32, #77) |
| `Sources/BabelOtterKit/Capture/ReplacementVerifier.swift` | Read-back-and-compare, pure (#33) |
| `Sources/BabelOtterKit/Capture/ResultCustody.swift` | A generated result is never dropped, pure (#34) |
| `Sources/BabelOtterApp/ClipboardCapture.swift` | `NSPasteboard` + synthetic ⌘C/⌘V with save/restore (#32, #33) |

---

### Task 1: NDJSON framing across packet boundaries (#41)

**Files:**
- Create: `Sources/BabelOtterKit/LLM/NDJSONFramer.swift`
- Test: `Tests/BabelOtterKitTests/LLM/NDJSONFramerTests.swift`

**Interfaces:**
- Produces: `struct NDJSONFramer: Sendable { mutating func consume(_ chunk: String) -> [String]; mutating func finish() -> [String] }`

**Design notes:**

Pure and CI-testable, which is the point of separating it from the client: #41's acceptance criterion "a partial line split across packets is buffered and parsed correctly once complete" is a framing property, not a networking one, and deserves a test that does not need a daemon.

`consume` returns only **complete** lines and retains the trailing partial in a buffer. `finish` returns whatever remains, so a final line without a newline is not silently dropped — the failure mode where the last token of a generation disappears.

Empty lines are skipped rather than returned: Ollama's NDJSON contains them, and returning them would push the "is this malformed?" decision onto every caller.

- [ ] **Step 1: Write the failing tests**

```swift
import Testing
@testable import BabelOtterKit

@Suite("NDJSON framing survives packet boundaries")
struct NDJSONFramerTests {

    @Test("a whole line in one chunk is returned")
    func wholeLine() {
        var framer = NDJSONFramer()
        #expect(framer.consume("{\"a\":1}\n") == ["{\"a\":1}"])
    }

    @Test("a line split across two chunks is buffered until complete")
    func splitLine() {
        var framer = NDJSONFramer()
        #expect(framer.consume("{\"a\":") == [])
        #expect(framer.consume("1}\n") == ["{\"a\":1}"])
    }

    @Test("a line split across three chunks still arrives once")
    func splitAcrossThree() {
        var framer = NDJSONFramer()
        #expect(framer.consume("{\"a") == [])
        #expect(framer.consume("\":") == [])
        #expect(framer.consume("1}\n") == ["{\"a\":1}"])
    }

    @Test("several lines in one chunk all come back, in order")
    func severalLines() {
        var framer = NDJSONFramer()
        #expect(framer.consume("{\"a\":1}\n{\"b\":2}\n") == ["{\"a\":1}", "{\"b\":2}"])
    }

    @Test("a trailing partial is kept, not emitted")
    func trailingPartialHeld() {
        var framer = NDJSONFramer()
        #expect(framer.consume("{\"a\":1}\n{\"b\"") == ["{\"a\":1}"])
    }

    @Test("finish returns the last line when the stream ends without a newline")
    func finishFlushes() {
        var framer = NDJSONFramer()
        _ = framer.consume("{\"a\":1}")
        #expect(framer.finish() == ["{\"a\":1}"])
    }

    @Test("finish on an empty buffer returns nothing")
    func finishEmpty() {
        var framer = NDJSONFramer()
        #expect(framer.finish() == [])
    }

    @Test("blank lines are skipped rather than surfaced as malformed")
    func blankLinesSkipped() {
        var framer = NDJSONFramer()
        #expect(framer.consume("{\"a\":1}\n\n{\"b\":2}\n") == ["{\"a\":1}", "{\"b\":2}"])
    }

    @Test("CRLF line endings frame correctly")
    func crlf() {
        var framer = NDJSONFramer()
        #expect(framer.consume("{\"a\":1}\r\n") == ["{\"a\":1}"])
    }
}
```

- [ ] **Step 2: Run to verify it fails** — `swift test --filter NDJSONFramerTests`.
- [ ] **Step 3: Implement `NDJSONFramer.swift`.**
- [ ] **Step 4: Run to verify it passes.**
- [ ] **Step 5: Plant a defect and watch it fail.** Make `consume` emit the trailing partial immediately. `trailingPartialHeld` and `splitLine` must fail. Revert.
- [ ] **Step 6: Add to the mutation list (pass 1) and commit.**

---

### Task 2: Ollama wire contracts and recorded fixtures (#41)

**Files:**
- Create: `Sources/BabelOtterKit/LLM/OllamaWire.swift`
- Create: `Tests/Fixtures/ollama/chat-stream.ndjson` — a recorded generation, synthetic
- Test: `Tests/BabelOtterKitTests/LLM/OllamaWireTests.swift`

**Interfaces:**
- Produces:
  - `struct ChatMessage: Sendable, Codable, Equatable { let role: String; let content: String }`
  - `struct ChatRequest: Sendable, Codable { let model: String; let messages: [ChatMessage]; let stream: Bool }`
  - `struct ChatChunk: Sendable, Codable, Equatable { let message: ChatMessage?; let done: Bool }`
  - `struct TagsResponse: Sendable, Codable { let models: [InstalledModel] }`
  - `struct PullProgress: Sendable, Codable, Equatable { let status: String; let completed: Int?; let total: Int? }`
  - `enum StreamEvent: Sendable, Equatable { case delta(String); case finished; case malformed(line: String) }`
  - `enum OllamaWire { static func event(from line: String) -> StreamEvent }`

**Design notes:**

`malformed(line:)` is an **event, not a thrown error**, because #41 requires a malformed line be *reported rather than silently skipped* while the rest of the stream continues. Throwing would abandon a generation over one bad line; skipping would hide a model that is misbehaving.

Fixtures are synthetic and live under `Tests/Fixtures/`, which `FixtureContentGuardTests` already scans for markers of real correspondence (#25). Do not paste a real generation in here.

- [ ] **Step 1: Write the failing tests** — decode a delta line, a `done` line, a line that is valid JSON of the wrong shape (→ `malformed`), a line that is not JSON (→ `malformed`), and replay the whole recorded fixture asserting the concatenated deltas equal the expected text and the sequence ends with `.finished`.
- [ ] **Step 2–4: fail, implement, pass.**
- [ ] **Step 5: Add to the mutation list (pass 1) and commit.**

---

### Task 3: The one networking call site (#41)

**Files:**
- Create: `Sources/BabelOtterKit/LLM/OllamaTransport.swift`
- Create: `Sources/BabelOtterKit/LLM/OllamaClient.swift`
- Test: `Tests/BabelOtterKitTests/LLM/OllamaClientTests.swift`
- Verify: `Config/networking-allowlist.txt` already names `OllamaClient.swift` — no edit needed

**Interfaces:**
- Consumes: `OllamaEndpoint`, Tasks 1 and 2.
- Produces:
  - `protocol OllamaTransport: Sendable { func lines(for request: URLRequest) async throws -> AsyncThrowingStream<String, any Error> }`
  - `struct URLSessionTransport: OllamaTransport` — the only type that touches the network
  - `struct FakeTransport: OllamaTransport` — replays fixture lines, in tests
  - `struct OllamaClient: Sendable { init(endpoint: OllamaEndpoint, transport: any OllamaTransport); func chat(_:) -> AsyncThrowingStream<StreamEvent, any Error>; func installedModels() async throws -> [InstalledModel]; func health() async -> DaemonStatus }`

**Design notes:**

The transport seam is what lets every behaviour in this file be tested in CI with no daemon: `FakeTransport` replays the Task 2 fixture. Only `URLSessionTransport` performs I/O, and it lives in this same file because the allowlist permits exactly one path.

The client takes an `OllamaEndpoint` and builds every URL with `endpoint.url(path:)`. It must never accept a `URL`, a host string or a port from a caller — that is what makes "cannot target a non-loopback host" a type-level property rather than a convention.

- [ ] **Step 1: Write the failing tests** — a fixture-backed chat yields the expected deltas then `.finished`; a transport error propagates; the built request's URL host is `127.0.0.1`; constructing a client cannot express a non-loopback host (a compile-level argument, asserted by the absence of such an initialiser plus an `OllamaEndpoint` test).
- [ ] **Step 2–4: fail, implement, pass.**
- [ ] **Step 5: Run the architecture guard** — `swift test --filter NetworkingCallSiteTests` must pass with exactly one allowlisted file.
- [ ] **Step 6: Add to the mutation list (pass 1) and commit.**

---

### Task 4: Timeout and cancellation (#47)

**Files:**
- Create: `Sources/BabelOtterKit/LLM/GenerationTimeout.swift`
- Test: `Tests/BabelOtterKitTests/LLM/GenerationTimeoutTests.swift`

**Design notes:**

The policy is pure — given elapsed time, configured timeout and whether the user dismissed, decide `.continue`, `.cancelTimedOut` or `.cancelUserRequested` — so the rule is provable without waiting 60 seconds in a test. The clock is injected.

`NFR-PERF-3` and `FR-UI-07` share one requirement worth stating: **a cancelled request writes nothing to history and applies no partial result.** Model that as a `CancellationOutcome` that explicitly carries "discard", so the call site cannot forget.

- [ ] **Step 1: Write the failing tests** — under the timeout continues; exactly at the timeout cancels (boundary, kills `>` vs `>=`); a user dismiss cancels regardless of elapsed time; a cancelled outcome always reports `discardPartialResult == true`; timeout comes from `Configuration.timeoutSeconds`, not a constant.
- [ ] **Step 2–4: fail, implement, pass.**
- [ ] **Step 5: Add to the mutation list (pass 1) and commit.**

---

### Task 5: Daemon health and recovery (#45)

**Files:**
- Create: `Sources/BabelOtterKit/LLM/DaemonStatus.swift`
- Test: `Tests/BabelOtterKitTests/LLM/DaemonStatusTests.swift`

**Interfaces:**
- Produces: `enum DaemonStatus: Sendable, Equatable { case ready; case unreachable(detail: String); case modelMissing(String) }` and a pure `DaemonStatusPolicy` mapping a probe result to a status and a menu-bar readiness level.

**Design notes:**

`FR-ONB-03`/`FR-UI-03` want three readiness levels — ready, degraded, blocked — and #45 wants recovery without a restart. Both are pure mappings and belong here rather than in whatever eventually polls.

"Never blocks the UI" is a property of the caller, not of this file; the note belongs in M1b's menu-bar task, and this plan states it so it is not lost.

- [ ] **Step 1: Write the failing tests** — a successful probe is `.ready`; a refused connection is `.unreachable` with a detail naming the endpoint; a configured model absent from `/api/tags` is `.modelMissing`; recovery from `.unreachable` to `.ready` needs no intermediate state; each status maps to the documented readiness level.
- [ ] **Step 2–4: fail, implement, pass.**
- [ ] **Step 5: Add to the mutation list (pass 1) and commit.**

---

### Task 6: Pulling a missing model (#46)

**Files:**
- Modify: `Sources/BabelOtterKit/LLM/OllamaClient.swift` — add `pull(model:)` streaming `PullProgress`
- Test: `Tests/BabelOtterKitTests/LLM/OllamaPullTests.swift`

**Design notes:**

This is the requirement most likely to be misread as an NFR-P2 violation, so state it in the code: babelOtter calls `POST 127.0.0.1:11434/api/pull`; **the Ollama daemon** makes the outbound connection. The app's own egress stays loopback-only, which is why #46's last acceptance criterion is that the architecture test still passes with no new call site.

Confirmation, name and size display, and cancellation are UI and belong to M3's onboarding; what lands here is the streaming progress API and the explicit no-new-call-site proof.

- [ ] **Step 1: Write the failing tests** — a fixture-backed pull yields increasing progress then completion; a declined pull makes no request at all; `NetworkingCallSiteTests` still reports exactly one call site.
- [ ] **Step 2–4: fail, implement, pass.**
- [ ] **Step 5: Commit.**

---

### Task 7: What a capture has to carry (#31)

**Files:**
- Create: `Sources/BabelOtterKit/Capture/SelectionSnapshot.swift`
- Test: `Tests/BabelOtterKitTests/Capture/SelectionSnapshotTests.swift`

**Interfaces:**
- Produces: `struct SelectionSnapshot: Sendable { let text: UserText; let tier: CaptureTier; let processIdentifier: pid_t; let bundleIdentifier: String?; let element: AXElementHandle?; let range: CFRange? }`

**Design notes:**

`text` is a `UserText`, not a `String` (NFR-P3): a snapshot is the single most log-attractive value in the app and must be unloggable without deliberate redaction.

`FR-CAP-01` requires the element, range, PID and bundle id be recorded **at trigger time**, because by the time the user presses Replace the frontmost application has changed — it is babelOtter's own popup.

`AXElementHandle` wraps `AXUIElement` so the kit does not export a CoreFoundation type across its API, and so tests can construct a snapshot without a real element.

- [ ] **Step 1–4: tests, fail, implement, pass** — a snapshot with empty text reports `isEmpty`; whitespace-only reports `isEmpty` (#35); the text does not appear in the snapshot's `description` (NFR-P3 — plant a `dump()` and watch the redaction hold).
- [ ] **Step 5: Add to the mutation list and commit.**

---

### Task 8: Tier selection by attempted capture, not by attribute probing (#31)

**Files:**
- Create: `Sources/BabelOtterKit/Capture/CaptureTier.swift`
- Create: `Sources/BabelOtterKit/Capture/AccessibilityReader.swift`
- Test: `Tests/BabelOtterKitTests/Capture/CaptureTierTests.swift`

**Interfaces:**
- Produces:
  - `enum CaptureTier: Sendable, Equatable, CaseIterable { case accessibilityText, textMarkerRange, clipboard }`
  - `protocol SelectionSource: Sendable { func read(tier: CaptureTier) -> String? }`
  - `struct TierLadder: Sendable { func capture(from source: any SelectionSource, allowing tiers: [CaptureTier]) -> (text: String, tier: CaptureTier)? }`

**Design notes:**

**This is the task spike #30 called its most misleading result.** VS Code advertises every selection attribute and returns empty from all of them. So the ladder's rule is: try a tier, and accept it **only if it returns a non-empty string**. Never ask what a tier supports.

Tier 2 is WebKit's parameterized attribute pair, `AXSelectedTextMarkerRange` → `AXStringForTextMarkerRange`. Without it Safari and Mail fall to the clipboard, which is both less private and less reliable.

The ladder itself is pure and takes a `SelectionSource`, so every ordering and fallback case is a CI test with a fake. `AccessibilityReader` is the real source and is exercised only by the local-only integration suite gated behind `BABELOTTER_INTEGRATION`.

- [ ] **Step 1: Write the failing tests** — tier 1 returning text wins; tier 1 empty falls to tier 2; **tier 1 and 2 both returning empty strings falls to tier 3 (the VS Code case)**; all tiers empty returns `nil`; a restricted tier list is honoured; tiers are attempted in ladder order and no later tier is consulted once one succeeds (recording fake).
- [ ] **Step 2–4: fail, implement, pass.**
- [ ] **Step 5: Plant the misleading defect and watch it fail.** Change the ladder to accept a tier that returns a non-`nil` but empty string. The VS Code case must fail. Revert. **Do not skip this** — it is the exact defect the spike found.
- [ ] **Step 6: Add to the mutation list and commit.**

---

### Task 9: Arming Electron accessibility ahead of time (#31)

**Files:**
- Create: `Sources/BabelOtterKit/Capture/ElectronAccessibility.swift`
- Test: `Tests/BabelOtterKitTests/Capture/ElectronAccessibilityTests.swift`

**Design notes:**

`AXManualAccessibility` returns `success` immediately but the tree builds **asynchronously**, so the first capture afterwards still fails. Arming at capture time therefore makes every Electron app look permanently unreachable — which is exactly what the first prototype concluded.

So this arms on **frontmost-application change**, and remembers which PIDs it has already armed so it does not repeat the call on every switch. The remembering is pure and testable; the AX call is behind a protocol.

State plainly in the doc comment that this switches on accessibility work inside applications we do not own. It is ordinary assistive-software behaviour and is what VoiceOver does, but it is a side effect that deserves naming rather than burying.

- [ ] **Step 1–4: tests, fail, implement, pass** — an unseen Electron PID is armed once; the same PID is not armed twice; a non-Electron app is never armed; a relaunched app with a new PID is armed again.
- [ ] **Step 5: Add to the mutation list and commit.**

---

### Task 10: Replacement that reads back before claiming success (#33)

**Files:**
- Create: `Sources/BabelOtterKit/Capture/ReplacementVerifier.swift`
- Test: `Tests/BabelOtterKitTests/Capture/ReplacementVerifierTests.swift`

**Design notes:**

**`AXUIElementSetAttributeValue` returns `.success` for writes that do nothing** — measured directly on Mail's read-only HTML view. A caller trusting the return code tells the user "Replaced" over untouched text. That is the precise failure #34 exists to prevent, with the false signal coming from the OS rather than from our code.

So a write is only successful when a read-back **equals what was written**. Anything else falls through to the next tier or surfaces the failure. The comparison is pure and belongs here; the write and the read are behind a protocol.

- [ ] **Step 1: Write the failing tests** — a write whose read-back matches succeeds; **a write returning `.success` whose read-back is unchanged fails** (the Mail case); a read-back that throws fails; a read-back differing only by trailing newline fails (be strict — the model's output is exact); success is never reported without a read-back having happened (recording fake).
- [ ] **Step 2–4: fail, implement, pass.**
- [ ] **Step 5: Plant the defect and watch it fail.** Make the verifier trust the return code. The Mail case must fail. Revert.
- [ ] **Step 6: Add to the mutation list and commit.**

---

### Task 11: The clipboard tier, with save and restore (#32)

**Files:**
- Create: `Sources/BabelOtterApp/ClipboardCapture.swift`
- Test: `Tests/BabelOtterKitTests/Capture/ClipboardPolicyTests.swift` (policy only)

**Design notes:**

`NSPasteboard` and synthetic key events need `AppKit`, so this file lives in the **app target**, not the kit — `NoUIImportsTests` enforces that boundary.

What can be tested in CI is the *policy*: save, copy, read, restore, and **restore even on the timeout path**. #32's third acceptance criterion is precisely the case a naive implementation gets wrong — it returns early on timeout and leaves the user's clipboard holding the captured text. Model the restore as a `defer`-shaped guarantee and test it with a fake pasteboard that records ordering.

The popup must indicate the less-private path was taken (#32, `NFR-P9`); that display is M1b's, and the snapshot already carries `tier` for it.

- [ ] **Step 1: Write the failing tests** — the original contents are restored after a successful capture; restored after a timeout with no text; restored after the read throws; the restore happens after the read, never before (ordering fake); a capture reports `tier == .clipboard` so the UI can say so.
- [ ] **Step 2–4: fail, implement, pass.**
- [ ] **Step 5: Plant an early return on the timeout path and watch the restore test fail.** Revert.
- [ ] **Step 6: Commit.**

---

### Task 12: Strict Accessibility-only mode (#77)

**Files:**
- Create: `Sources/BabelOtterKit/Capture/CapturePolicy.swift`
- Modify: `Sources/BabelOtterKit/Config/Configuration.swift` — add `strictAccessibilityOnly: Bool`
- Modify: `PRIVACY.md` — correct the accepted-risk section
- Test: `Tests/BabelOtterKitTests/Capture/CapturePolicyTests.swift`

**Design notes:**

Pulled from M4 into M1a because spike #30 showed the clipboard is the common path, not the rare one. Landing it in the same milestone as the fallback it guards means the gap never ships unguarded.

The policy is pure: given configuration, return the tiers the ladder may attempt. In strict mode that is tiers 1 and 2 only, and a capture that finds no tier reports **"this application cannot be read without the clipboard"** rather than silently doing nothing — a refusal the user can act on.

`Configuration.strictAccessibilityOnly` defaults to `false`, preserving current behaviour; making it the default is a product decision, not this task's.

**`PRIVACY.md` must be corrected here.** It currently accepts the Universal Clipboard exposure on the stated assumption that the clipboard is a rare fallback. That assumption is measured false. The document should say the clipboard is the only path for Electron and for replacement into web content, and point at this setting.

- [ ] **Step 1: Write the failing tests** — default configuration allows all three tiers; strict mode allows exactly tiers 1 and 2; strict mode with no AX tier available yields an explicit refusal naming the application, not an empty result; the refusal is distinguishable from an empty selection (#35), because they need different messages.
- [ ] **Step 2–4: fail, implement, pass.**
- [ ] **Step 5: Update `PRIVACY.md` and commit together**, so the document and the mechanism change in one commit.
- [ ] **Step 6: Add to the mutation list.**

---

### Task 13: Never losing a generated result (#34)

**Files:**
- Create: `Sources/BabelOtterKit/Capture/ResultCustody.swift`
- Test: `Tests/BabelOtterKitTests/Capture/ResultCustodyTests.swift`

**Design notes:**

#34's third criterion — "no path discards a completed result" — is a claim about *every* error path, which is only provable if the paths are enumerable. So model the outcome as a closed enum and write an exhaustive test over its cases asserting each one still carries the result and still offers Copy.

That is the same shape as `BlockCountPolicy`'s exhaustion test in the core plan, and for the same reason: a property claimed over all paths should be checked over all paths, not spot-checked.

- [ ] **Step 1: Write the failing tests** — exhaustively over every `ReplacementFailure` case: the result is retained and Copy is offered; the source application having quit still yields a shown result; a cancelled generation (Task 4) is the one case with nothing to retain, and is distinct from a failure.
- [ ] **Step 2–4: fail, implement, pass.**
- [ ] **Step 5: Add to the mutation list and commit.**

---

### Task 14: Empty and whitespace-only selections (#35)

**Files:**
- Modify: `Sources/BabelOtterKit/Capture/CapturePolicy.swift`
- Test: extend `CapturePolicyTests`

**Design notes:**

Cheap, and worth its own task only because it must come *before* the model is called: #35's point is that nothing is requested and nothing is written to history. A whitespace-only selection counts as empty, consistent with `LanguageDetector`'s non-whitespace length measure in the core plan — the two must agree or a selection can be "long enough to detect" and "empty" at once.

- [ ] **Step 1–4: tests, fail, implement, pass** — empty reports empty; whitespace-only reports empty; a newline-and-tab-only selection reports empty; no request is issued (recording fake); nothing is written to history.
- [ ] **Step 5: Commit.**

---

### Task 15: Integration suite and milestone close

**Files:**
- Create: `Tests/BabelOtterKitTests/Integration/CaptureIntegrationTests.swift` — gated by the existing `IntegrationGate`
- Modify: `docs/architecture.md` — record anything these measurements contradict

**Design notes:**

These tests need a granted Accessibility permission and real applications, so they are local-only behind `BABELOTTER_INTEGRATION`, exactly as `IntegrationGate` and the spec's testing table already provide for. CI never runs them; that is intentional, not a gap.

**Run these on the ZHAW machine early.** `docs/architecture.md` §7 lists "whether a managed ZHAW Mac permits the Accessibility grant at all" as unmeasured, and notes that if MDM blocks it, no tier works and M1b's premise fails. That is a milestone-level risk, and it is cheap to answer.

- [ ] **Step 1: Write the integration tests** — TextEdit round-trips tier 1 read and write; Safari reads via tier 2; VS Code falls to tier 3; Mail's read-only view reports a *failed* replacement rather than a false success; an Electron app captures on the second attempt after arming.
- [ ] **Step 2: Run them locally with `BABELOTTER_INTEGRATION=1`** and record the results.
- [ ] **Step 3: Update `docs/architecture.md` §7** with anything that contradicts the spike, and say so plainly rather than quietly editing the table.
- [ ] **Step 4: Confirm the full suite and mutation gate pass, then push and watch CI.**
- [ ] **Step 5: Close #31, #32, #33, #34, #35, #41, #45, #46, #47, #77 and the M1a milestone.**

---

## Self-review

**Spec coverage.** §4.1 `FR-CAP-01` → Tasks 7, 8, 9; `FR-CAP-02` → Task 11; `FR-CAP-03`/`04` → Tasks 10, 11; `FR-CAP-05` → Task 13; `FR-CAP-06` → Task 14. §4.13 `FR-OLL-01`/`02` → Task 5; `FR-OLL-03` → Task 6; `FR-OLL-04` → Tasks 1–3. §5.1 `NFR-P2`/`P4` → Tasks 3, 6; `NFR-P9` → Tasks 11, 12. §5.3 `NFR-PERF-3` → Task 4. §5.4 `NFR-REL-3` → Task 13. §4.9 `FR-UI-07` → Task 4 (policy; the key handling is M1b).

**Deliberately not here.** The popup, hotkeys, menu bar and the display of tier/profile/inference are M1b. Glossary *editing* is M3. This plan produces no UI.

**Placeholder scan.** No TBDs. Every task names exact files and concrete test cases. Tasks 1 and 2 carry full test code; Tasks 3–15 carry enumerated cases with expected outcomes, because their fakes are defined by the interfaces block above them.

**Type consistency.** `CaptureTier` is defined in Task 8 and consumed in Tasks 7, 11, 12. `SelectionSnapshot` is produced in Task 7 and consumed in Tasks 10, 13. `StreamEvent` is defined in Task 2 and consumed in Task 3. `DaemonStatus` is defined in Task 5 and returned by `OllamaClient.health()` in Task 3 — Task 3 must land the signature and Task 5 the policy, so build Task 5's enum first if executing out of order.
